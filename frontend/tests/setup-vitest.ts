// G7 — Vitest setup file. Imports jest-dom matchers (toBeInTheDocument,
// toHaveClass, etc.) so component tests in later phases can assert on
// the DOM with the standard library vocabulary.

import "@testing-library/jest-dom/vitest";
